<div class="d-flex flex-column align-items-center mt-3">
  <h1><%= "#{@user.first_name} #{@user.last_name}" %></h1>
  <p><%= @user.email %></p>
  <p><%= @user.description %></p>
  <div class="btn-group" role="group" aria-label="Modifier infos importantes et profil">
    <%= link_to "Modifier mes informations de connexion", edit_user_registration_path, class: "btn btn-primary" %>
    <%= link_to "Editer mon profil", edit_user_path(current_user), class: "btn btn-primary" %>
  </div>

  <h2 class="mt-3">Tes événements</h2> 
</div>

<%= render partial: 'application/events_lists', locals: { object: @user.events } %>


